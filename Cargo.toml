[package]
name = "cuda-did"
version = "0.1.0"
edition = "2021"
description = "Decentralized Identity for agents — DID/SPIFFE-like cryptographic vessel identities with attestation and verification"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
